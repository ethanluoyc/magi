[build-system]
requires = ["setuptools >= 40.6.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.isort]
profile = "google"
src_paths = ["magi"]
known_first_party = ["magi"]

[tool.yapf]
based_on_style = "yapf"
indent_width = 2
